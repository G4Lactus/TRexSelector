% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{[<-.mmap_matrix}
\alias{[<-.mmap_matrix}
\title{Assign elements or blocks in an mmap_matrix}
\usage{
\method{"["}{mmap_matrix}(x, i, j) <- value
}
\arguments{
\item{x}{mmap_matrix object (must be in \code{"readwrite"} mode)}

\item{i}{Row index or contiguous range (1-based)}

\item{j}{Column index or contiguous range (1-based)}

\item{value}{A scalar or numeric matrix whose dimensions match the selected block}
}
\value{
\code{x} invisibly (required by R's replacement function protocol)
}
\description{
Writes a scalar or a numeric matrix block into the memory-mapped file.
The object must be open in \code{"readwrite"} mode.
}
\examples{
\donttest{
mat <- matrix(as.double(1:20), nrow = 4, ncol = 5)
f <- tempfile(fileext = ".bin")
on.exit(unlink(f), add = TRUE)
mm <- convert_to_memory_mapped(mat, f)
mm[2, 3] <- 99.0
mm[1:2, 1:3] <- matrix(as.double(1:6), nrow = 2, ncol = 3)
}

}
