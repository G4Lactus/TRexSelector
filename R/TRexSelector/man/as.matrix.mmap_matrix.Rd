% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as.matrix.mmap_matrix}
\alias{as.matrix.mmap_matrix}
\title{Get Zero-Copy View as Numeric Vector (Block)}
\usage{
\method{as.matrix}{mmap_matrix}(x, ...)
}
\arguments{
\item{x}{mmap_matrix object}

\item{...}{extra arguments including row_range and col_range}
}
\value{
Numeric matrix (copied)
}
\description{
Get Zero-Copy View as Numeric Vector (Block)
}
\examples{
\donttest{
mat <- matrix(as.double(1:20), nrow = 4, ncol = 5)
f <- tempfile(fileext = ".bin")
on.exit(unlink(f), add = TRUE)
convert_to_memory_mapped(mat, f)
mm <- mmap_matrix(f, rows = 4, cols = 5)
as.matrix(mm)
}

}
